apiVersion: 1

providers:
  - name: "ServiceMatrix Dashboards"
    orgId: 1
    folder: "ServiceMatrix"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
