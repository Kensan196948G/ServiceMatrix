apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicematrix.fullname" . }}-backend
  namespace: {{ include "servicematrix.namespace" . }}
  labels:
    {{- include "servicematrix.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servicematrix.selectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
